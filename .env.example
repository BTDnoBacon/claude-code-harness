# Copy this file to .env and fill in your webhook URLs.
# .env is gitignored — never commit real URLs.

# Discord: Server Settings → Integrations → Webhooks → Copy Webhook URL
DISCORD_WEBHOOK_URL=

# Slack: api.slack.com → Your Apps → Incoming Webhooks → Webhook URL
SLACK_WEBHOOK_URL=
